# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# --- App Environment ---
NODE_ENV=development

# --- API Service ---
PORT=3000
ROUTE_PREFIX=
JWT_SECRET=your_jwt_secret_here
REFRESH_SECRET=your_refresh_secret_here
GOOGLE_CLIENT_ID=your_google_client_id_here
CORS_ORIGINS=https://app.example.com,https://www.example.com
UPLOAD_TEMP_DIR=/tmp

# --- ML Service ---
# The API service will use this URL to communicate with the ML service
YOLO_SERVICE_URL=http://ml-service:8000

# --- Database Service ---
# Credentials for the database container itself
MYSQL_ROOT_PASSWORD=your_root_password_here
MYSQL_USER=snapandgo_user
MYSQL_PASSWORD=your_user_password_here
MYSQL_DATABASE=snapandgo

# --- Database Connection URL for the API Service ---
# This uses the variables above to build the connection string.
# The API container will connect to the 'db' service on the default mysql port.
DATABASE_URL="mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@db:3306/${MYSQL_DATABASE}"
